{
	"name": "artek-mail-worker",
	"version": "1.0.0",
	"description": "Serverless contact form email handler built on Cloudflare Workers with Resend API and Turnstile bot protection",
	"author": "Rıza Emre ARAS",
	"license": "AGPL-3.0-or-later",
	"scripts": {
		"deploy": "dotenv -- wrangler deploy",
		"dev": "dotenv -- wrangler dev --port 8788",
		"start": "dotenv -- wrangler dev --port 8788",
		"cf-typegen": "dotenv -- wrangler types"
	},
	"overrides": {
		"sharp": "0.35.3"
	},
	"devDependencies": {
		"dotenv-cli": "11.0.0",
		"typescript": "5.9.3",
		"wrangler": "4.112.0"
	},
	"dependencies": {
		"resend": "6.18.0"
	}
}
